---
owner: dev-senior
revisado-em: 2026-05-26
status: stable
story: US-111
ac-fechados-nesta-sessao: [AC-111-3]
audit_sha: e106faf7d2fdd5b0f05c5bca4ab420e727660911ccf8eb6c311a22db949a231d
session: default
---

# CHK-2026-05-26 — Release v2.0.0 fecha AC-111-3 (bump de versao)

## Proposito em 1 frase

Bumpar `package.json` 1.3.1 → 2.0.0 + sincronizar `plugin.json` + `continue/config.yaml` + escrever CHANGELOG e release notes — fecha AC-111-3 e materializa o ciclo PRD-003.

## O que muda pro cliente (nao-programador)

- Cliente que rodar `npx roldao-method@latest` agora recebe v2.0.0.
- CHANGELOG explica em PT-BR claro tudo que mudou nas 6 stories.
- Release notes `docs/releases/v2.0.0.md` orienta como aplicar e o que prestar atencao (breaking change + flag de compat).

## Non-goals respeitados

- Nao mexe em codigo de producao alem dos arquivos de versao.
- Nao remove nenhum agente/hook/command (ADR-031).
- Nao publica no npm (depende de credenciais do Roldao).
- Nao cria tag git ainda — fica como proxima micro-task se ele autorizar.

## Riscos

| Risco | Mitigacao |
|---|---|
| Cliente em v1.x quebra com marker antigo | Flag `ROLDAO_METHOD_LEGACY_MARKERS=1` ate v2.2.0 (ADR-021) |
| Cliente nao saber que mudou | CHANGELOG bloco "O que muda pra voce" lista os 8 efeitos visiveis |
| Major bump confundir scripts CI | Documentado em `docs/releases/v2.0.0.md` secao "Como aplicar" |

## Decisoes dos auditores

- **Caio (auditor-seguranca):** APROVADO. Diff so prosa + bump. Zero secret/PII/URL hardcoded. Supply chain intacto (sem mudanca em deps).
- **Julia (auditor-qualidade):** APROVADO. Versao 2.0.0 sincronizada nos 4 arquivos. CHANGELOG segue template CHG (bloco "O que muda pra voce" obrigatorio em major presente). Release notes segue template REL. Drift de plugin-manifest detectado e corrigido.
- **Pedro (auditor-produto):** APROVADO PRO CLIENTE. PT-BR sem jargao cru. Narrativa honesta (42/44 ACs entregues, nao infla "100%"). AC-116-7 como debito conhecido (citado no CHANGELOG + secao "Atencao" do release notes). Major bump justificado.

## Status final

| AC | Status |
|---|---|
| AC-111-3 | **ENTREGUE** — bump 2.0.0 sincronizado em package.json + plugin.json + continue.yaml |

## Proximo passo

Tag git `v2.0.0` (`git tag v2.0.0`) + opcionalmente push + opcionalmente release no GitHub via `gh release create v2.0.0`. AC-116-7 (validacao ao vivo 5 tarefas-tipo com Roldao) fica como debito conhecido pra proxima sessao.
